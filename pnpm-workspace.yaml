packages:
  - "apps/web"
  - "packages/*"

# Explicit supply-chain allowlist for native binaries used by the frontend
# toolchain. Every other dependency install script remains blocked.
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
