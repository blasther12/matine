# Shared contracts

This directory is reserved for generated, non-secret API contracts shared by the
web and API applications. It intentionally contains no runtime package yet: the
OpenAPI document will remain the source of truth until sharing code removes real
duplication.
